'use strict';

function grouping_count(collection) {
  //在这里写入代码
  // let countMap = new Map();
  // collection.forEach(i => {
  //   const baseCount = countMap.has(i) ? countMap.get(i) : 0;
  //   countMap.set(i, baseCount + 1);
  // });
  // console.log(countMap);
  // return countMap;
}

module.exports = grouping_count;
